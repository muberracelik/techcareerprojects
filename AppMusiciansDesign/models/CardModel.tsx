class CardModel {
  text: string;
  color: string;
  number: string;
  constructor(text: string, color: string, number: string) {
    this.text = text;
    this.color = color;
    this.number = number;
  }
}

export default CardModel;